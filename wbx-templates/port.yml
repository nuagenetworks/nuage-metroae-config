name: Port
description: Create a Port
template-version: 1.0
engine-version: 1.0
software-type: Nuage Networks WBX
software-version: 9.9.9
variables:
  - name: identifier
    type: string
  - name: description
    type: string
  - name: shutdown
    type: boolean
  - name: speed
    type: integer
  - name: mtu
    type: integer
actions:
  - create-object:
      type: Port
      select-by-field: identifier
      actions:
        - set-values:
            identifier: {{ identifier }}
            description: {{ description }}
            shutdown: {{ shutdown }}

        - create-object:
            type: Ethernet
            select-by-field: speed
            actions:
              - set-values:
                  speed: {{ speed }}
                  mtu: {{ mtu }}
